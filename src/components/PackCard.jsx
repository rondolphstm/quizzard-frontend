

export default function PackCard ({pack, index}){

    return(
        <div>
            <h4>{pack.title}</h4>
            <p>{pack.description}</p>
            <p>
            </p>
        </div>
    )



}